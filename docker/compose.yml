# Docker Compose for repo-auditor
# Usage: docker compose -f docker/compose.yml up

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: "repo-auditor"
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    restart: unless-stopped
